[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegame"
version = "0.1.0"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
include = ["*"]
exclude = []
namespaces = true